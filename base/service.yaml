apiVersion: v1
kind: Service
metadata:
  name: searxng-mcp
  namespace: searxng-mcp
  labels:
    app: searxng-mcp
spec:
  selector:
    app: searxng-mcp
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
